description "Marathon scheduler for Mesos"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

exec /usr/bin/marathon{% if groups.marathon|count > 1 %} --ha{% endif %} --hostname {{ ansible_eth1.ipv4.address }}